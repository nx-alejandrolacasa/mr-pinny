{
  "name": "mr-pinny-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "npm run dev:firefox",
    "dev:firefox": "npm run dev -w @mr-pinny/firefox",
    "dev:chrome": "npm run dev -w @mr-pinny/chrome",
    "build": "npm run build -w @mr-pinny/firefox && npm run build -w @mr-pinny/chrome",
    "build:firefox": "npm run build -w @mr-pinny/firefox",
    "build:chrome": "npm run build -w @mr-pinny/chrome",
    "lint": "npm run lint --workspaces --if-present",
    "test": "node scripts/test-pins.mjs",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
