{
  "name": "@mr-pinny/chrome",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Chrome extension that keeps your pinned tabs on the URL you chose: restore them on demand, when idle or on startup, and open their links in new tabs.",
  "scripts": {
    "build": "tsx build.ts",
    "dev": "tsx build.ts && (tsx build.ts --watch & web-ext run --source-dir=dist --target=chromium)",
    "package": "web-ext build"
  },
  "dependencies": {
    "@mr-pinny/shared": "*"
  },
  "devDependencies": {
    "web-ext": "^10.1.0"
  }
}
